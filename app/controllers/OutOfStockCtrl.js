webshopControllers.controller('OutOfStockCtrl', ['$scope',
    function($scope){

        // the type of products, not in stock at the moment

        $scope.productsType = 'Computers, TVs and Game Consoles';
    }
]);